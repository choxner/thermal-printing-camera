// set up I2C communications
// add in all the libraries that are needed


static void captureImage(uint16_t width, uint16_t height)	{
	uint16_t lg2;
	while (height--) {
		lg2 = width;
		while(lg2--) {
			while(())
		}
	}
}